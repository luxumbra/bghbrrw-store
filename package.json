{
  "name": "badgerstore-monorepo",
  "version": "0.0.1",
  "description": "Badgerstore Monorepo",
  "author": "FRST.uk",
  "private": true,
  "devDependencies": {
    "concurrently": "^8.0.1",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "dev": "concurrently \"pnpm --filter @badgerstore/backend dev\" \"pnpm --filter @badgerstore/frontend dev\"",
    "build": "pnpm --recursive run build"
  }
}
